import { Plandevalidationmodule } from './plandevalidationmodule';

describe('Plandevalidationmodule', () => {
  it('should create an instance', () => {
    expect(new Plandevalidationmodule()).toBeTruthy();
  });
});
