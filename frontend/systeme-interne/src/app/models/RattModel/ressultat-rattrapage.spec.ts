import { RessultatRattrapage } from './ressultat-rattrapage';

describe('RessultatRattrapage', () => {
  it('should create an instance', () => {
    expect(new RessultatRattrapage()).toBeTruthy();
  });
});
